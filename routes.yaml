routes:
  /: home

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/

collections:
  /portfolio/:
    permalink: /portfolio/{slug}/
    template: projects
    filter: tag:project
    data: tag.project
  /blog/:
    permalink: /blog/{slug}/
    template: blog
  